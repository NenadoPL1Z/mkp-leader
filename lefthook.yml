pre-commit:
  commands:
    lint:
      run: eslint . --max-warnings=0
    type-check:
      run: tsc --noEmit --skipLibCheck
pre-push:
  commands:
    lint:
      run: eslint . --max-warnings=0
    type-check:
      run: tsc --noEmit --skipLibCheck
