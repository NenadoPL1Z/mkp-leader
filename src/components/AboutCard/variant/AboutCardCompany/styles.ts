import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    gap: 5,
    marginLeft: 8,
    flexDirection: "column",
    alignItems: "flex-end",
  },
  wrapper: {
    gap: 4,
    flexDirection: "row",
    alignItems: "center",
  },
});
