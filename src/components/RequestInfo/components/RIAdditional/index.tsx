import React from "react";
import { View, Text } from "react-native";

const RIAdditional = () => {
  return (
    <View>
      <Text>123</Text>
    </View>
  );
};

export default React.memo(RIAdditional);
