import React from "react";
import { Text, View } from "react-native";

const RIComments = () => {
  return (
    <View>
      <Text>123</Text>
    </View>
  );
};

export default React.memo(RIComments);
