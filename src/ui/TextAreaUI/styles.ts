import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {},
  inputStyle: {
    height: "auto",
    minHeight: 80,
    maxHeight: 250,
    paddingTop: 10,
    paddingBottom: 10,
    textAlignVertical: "top",
  },
  descriptionInfo: {
    marginTop: 8,
  },
});
